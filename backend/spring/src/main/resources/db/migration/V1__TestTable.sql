CREATE TABLE test (
                countyName VARCHAR(100) NOT NULL PRIMARY KEY,
                fid INTEGER NOT NULL,
                confirmedCovidCases INTEGER,
                populationProportionCovidCases INTEGER,
                timeStampDate INTEGER,
                dateString VARCHAR (20)
);